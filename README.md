



 1) What is the difference between var, let, and const?
  ans: var is a variable where  we can change variable name and value both but let is a variable where we can only change
   variable value and const is a variable where we can change nothing value or variable name.
 2) What is the difference between map(), forEach(), and filter()? 
ans:forEach is use for looping elements and  it returns undefined,map() create a new array from an array and returns new
array with not changing real array, filter() returns an array which are matched the condition.
 3) What are arrow functions in ES6?
ans:  example: const newModel=()=>{
}; it concise a function.
 4) How does destructuring assignment work in ES6?
ans:it helps to find a data from an object or an array ans also helps assign a data in specific variable.
 5) Explain template literals in ES6. How are they different from string concatenation?
ans: template literals is one kind of syntax which started with ``  . with the help of this we can write multi-line string. with 
the help of this we can write code dynamically.
   









